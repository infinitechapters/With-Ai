
services:
  backend:
     build: ./backend
     ports:
        - "3000:3000"
     env_file:
        - ./backend/.env
  frontend:
    build: ./frontend 
    ports:
      - "5173:80"
    environment:
      - VITE_BASE_URL=http://localhost:3000